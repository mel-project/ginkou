import * as chai from 'chai';
import * as utils from '../src/utils';
//const { tap_faucet, send_mel, confirm_tx, new_wallet } = require('./utils.ts');
//const mocha = require('mocha');

/*
describe('MelWalletd', () => {
    describe('list wallets', () => {
        it('should return empty list', () => {
            true
        });
    });
});

modules.exports = {};
*/
